'use strict';

/*
 * main.js
 * -------
 * Inicialización global del sitio Sánchez e Hijos SpA.
 * Se carga en las 5 páginas del sitio.
 *
 * Sprint 2 — Tarea 12: Setup de la estructura JavaScript.
 * En esta tarea solo deja el andamiaje DOMContentLoaded listo,
 * sin lógica funcional. Las funcionalidades específicas viven en
 * validaciones.js (contacto) y portafolio.js (portafolio).
 */

document.addEventListener('DOMContentLoaded', function () {
  // Marca de vida para verificar que main.js se carga correctamente
  // en todas las páginas durante el desarrollo del Sprint 2.
  // Se removerá o reemplazará por inicializaciones reales en tareas posteriores.
  console.log('[Sánchez e Hijos] main.js cargado correctamente.');
});